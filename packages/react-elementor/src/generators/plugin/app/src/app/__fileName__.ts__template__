import {CustomProviders, <%=className%>Input, <%=className%>Title, WebComponentWrapper,} from '@<%=npmScope%>/ui';

WebComponentWrapper(
  '<%= fileName %>-input',
  <%=className%>Input,
  CustomProviders
);

WebComponentWrapper(
  '<%= fileName %>-title',
  <%=className%>Title,
  CustomProviders
);
